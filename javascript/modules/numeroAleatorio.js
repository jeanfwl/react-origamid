// usando deste modo, podemos importar diretamente com qualquer nome. Como por exemplo: import qualquerNumero from '...' que vai importar a função numeroAleatorio().
export default function numeroAleatorio() {
  return Math.random();
}
