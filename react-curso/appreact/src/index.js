import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './reacthooks/exercicios/3/ex3';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
